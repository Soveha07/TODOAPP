<p style="color: green"><%= notice %></p>

<%= render @task %>

<div>
  <%= link_to "Edit this task", edit_task_path(@task) %> |
  <%= link_to "Back to tasks", tasks_path %>

  <%= link_to "Delete", task_path(@task), method: :delete, data: { confirm: "Are you sure?", turbo: false }, class: 'btn btn-outline-danger text-white' %>

</div>
